/** Nomes dos ícones SVG (preto e branco via currentColor) — use em menuConfig */
export type MenuIconName =
  | 'dashboard'
  | 'folder'
  | 'box'
  | 'users'
  | 'chart'
  | 'settings'
  | 'sales';
